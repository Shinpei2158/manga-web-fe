export type CommentOption = {
  id: string;
  title: string;
};

export type CommentMe = {
  userId?: string;
  email?: string;
  role?: string;
};

export type CommentQuickMangaChip = {
  id: string;
  title: string;
  count: number;
};

